{% extends 'base.html.twig' %}
    {% block title %}Conference guestbook{% endblock %}
{% block body %}
    <h2>give ur feedback</h2>

    {% for conference in conferences %}
        <h4>{{ conference }}</h4>
        <p>
        <a href="{{ path('conference', {id: conference.id }) }}"> view</a>
        </p>
    {% endfor %}

{% endblock %}
